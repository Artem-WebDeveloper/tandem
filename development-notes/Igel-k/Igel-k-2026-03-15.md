# Наполнение БД и реализация фильтрации

## Задачи:

* Заполнить БД актуальными значениями
* Продумать фильтрацицю

## Ход работы:

#### Наполнение БД

Так как переносить данные из json в бд занятие скучное, я перепоручила это ии. В моём распоряжении gemini pro версии и я дала ей файлы и попросила написать sql запросы, чтобы не заниматься этим вручную. В целом, спустя несколько итераций получилось всё настроить и вот уже данные для двух квизов сервер отдаёт в общее пользование.

#### Фильтрация

Тут пока приходится думать как правильно реализовать: по каким полям вообще делать фильтрацию. Это мне задачка на сегодня и немного на завтра.

**Итог:** Есть уже готовое API для двух квизов. Далее будут остальные.
