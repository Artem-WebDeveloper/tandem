## 2026-03-21

### Что было сделано?

Сегодня работал с `Library`, перестраивая его на "апишные" рельсы, чтобы отображать все данные с бека, вместо моков, а так же сделал пагинацию, которая теперь ориентрируется на строку `url`. Для этого внедрил `useSearchParams` в компонент, который следит за url и меняет параметры строки при изменении страницы, после чего компонент ререндерится и `useEffect` отслеживает переменную страницы в зависимостях и внутри вызывает `fetch` функцию, делающую GET-запрос на бэк с нужным параметром.  
Теперь надо сделать похожее с фильтрами.  
Так же провели встречу с командой, где обсудили насущные вопросы и текущие задачи.

### Итоги

- Создание пагинации и настройка интеграции с Api
- Встреча с командой

---

### План

- Сделать функциональность фильтров
