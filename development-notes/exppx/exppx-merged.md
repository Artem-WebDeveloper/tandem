# 2026-02-16 (Code Completion Widget)

## Список задач на сегодня

- [ ] Придумать макет квиза;
- [ ] Разработать JSON-схему для информации о квизе;
- [ ] Подобрать компоненты Material UI для оформления компонента квиза.

## Ход работы

Для начала решил, что нужно дать пользователю информацию о том, какой сейчас квиз запущен и на какой сложности, а также возможность покинуть квиз. Эта информация является общей и не зависит от квиза, поэтому может быть вынесена на общую страницу (надо предложить это команде).

Для самого квиза было решено выделить следующие компоненты:

- номер вопроса вида 4/15;
- оставшееся время;
- строку прогресса (для более наглядного отображения прогресса);
- вопрос (в случае с моим квизом подсказку);
- зону задания (где будет код и поле для ввода пропуска);
- кнопки "Назад" и "Вперед" для перемещения по вопросам.

Примерный вид компонента на странице получисля таким:  
<img src='./images/quiz-template.png' width='700px'>

Задумался о том, что финальный экран с итогами квиза можно сделать общим компонентом (надо обсудить с командой).  
Примерный вид, думаю, должен быть таким:  
<img src='./images/result-screen.png' width='700px'>

Дальше JSON-схема.
За основу возьму пример с описания задания на GitHub:

```json
{
  "id": "cc-001",
  "type": "code-completion",
  "version": 1,
  "difficulty": 2,
  "tags": ["array-methods", "filter"],
  "payload": {
    "code": "const result = arr.___(x => x > 0);",
    "blanks": ["___"],
    "hints": [
      {
        "ru": "Этот метод возвращает новый массив с элементами, прошедшими проверку",
        "en": "This method returns a new array with elements that pass the test"
      }
    ]
  }
}
```

Для начала, разобрался, зачем нужна версия (для обратной совместимости, при переходе на новый формат).  
Данный пример содержит различные варианты подсказаок для разных локалей, что в нашем слкчае не нужно, а также представляет собой всего один вопрос, я же хочу, чтобы в одном квизе было несколько вопросов (5-15 штук). А еще здесь отсутствует информация о разделе, к которому относится задание.  
По итогу пришел к выводу, что JSON-схема должна выглядеть примерно вот так:

```json
{
  "id": "cc-001",
  "section": "Core JS",
  "type": "Code Completion",
  "difficulty": 2,
  "timeLimit": 180,
  "tags": ["array-methods"],
  "version": 1,
  "questions": [
    {
      "code": "const result = arr.___(x => x > 0);",
      "blanks": ["___"],
      "hint": "This method returns a new array with elements that pass the test",
      "answer": "filter"
    }
  ]
}
```

Задумался о том, нужно ли нам где-то на странице отображать теги (их тоже можно сделать общим компонентом, так как они будут нужны, как минимум, в библиотеке квизов (предложить команде)). Думаю можно расположить где-то в районе названий раздела и квиза. Решу позже, когда накидаю более конкретный дизайн.

Осталось согласовать её с Кариной (разработчиком бекэнда).

Слышал, что в Figma можно использовать компоненты Material UI для создания дизайна. Буду пробовать разобраться, чтобы набросать более конкретный вид для квиза и, заодно, познакомлюсь с Material UI.  
Как оказалось, в бесплатной версии набор UI-китов сильно ограничен, а набор Material 3 едва ли похож на Material UI. Придется ограничиться простым сопоставлением элемент-компонент без визуального примера. Для этого пойду изучать документацию по Material UI.

Сопоставление элементов интерфейса с компонентами Material UI:

- Номер вопроса - Box;
- Таймер - Box;
- Строка прогресса - LinearProgress;
- Вопрос - Box + TipsAndUpdatesTwoToneIcon для подсказки;
- Задание - Box + простой тег `<pre>` для кода + TextField для ответа
- Кнопки - Button

## Итоги дня

### Выполненные задачи

- [x] Придумать макет квиза;
- [x] Разработать JSON-схему для информации о квизе;
- [x] Подобрать компоненты Material UI для оформления компонента квиза.

### Проблемы

- Необходимость согласования вопросов по общим компонентам с командой;
- Отсутствие констркутора интерфейсов с дизайном из Material UI;

# 2026-02-17 (Code Completion Widget)

## Список задач на сегодня

- [ ] Разработать JSON-схемы для отправки ответов и получения результатов теста;
- [ ] Создать моковые данные для квиза;
- [ ] Разработать начальный вид и логику компонента теста.

## Ход работы

Вспомнил, что мы обсуждали процесс проверки ответов пользователя на недавнем созвоне. По ходу обсуждения были выдвинуты различные варианты проверки ответов, поэтому решил уточнить этот момент сейчас еще раз.  
По итогу было решено проверять ответы на бекэнде, потому что это самый правильный подход, так как он изолирует ответы от пользователя. Для этого нужно немного изменить JSON-схему с вопросами (удалить поле "answer" в вопросах). Также решил добавить вопросам "id", чтобы их можно было перемешивать.

Финальный (надеюсь 😅) вид схемы с вопросами:

```json
{
  "id": "cc-001",
  "section": "Core JS",
  "type": "Code Completion",
  "difficulty": 2,
  "timeLimit": 180,
  "tags": ["array-methods"],
  "version": 1,
  "questions": [
    {
      "id": 1
      "code": "const result = arr.___(x => x > 0);",
      "blanks": ["___"],
      "hint": "This method returns a new array with elements that pass the test"
    }
    ...
  ]
}
```

Схема с ответами пользователя:

```json
{
  "taskId": "cc-001",
  "userAnswers": [
    {
      "questionId": 1,
      "payload": "filter"
    },
    {
      "questionId": 2,
      "payload": "map"
    }
  ]
}
```

Схема с результатами:

```json
[
  {
    "questionId": 1,
    "payload": true
  },
  {
    "questionId": 2,
    "payload": false
  }
]
```

Сложно понять, как должна выглядеть схема, потому что хочется, чтобы бекэндеру было удобно, а я не имею опыта работы с бекэндом и не знаю, какие поля нужны. И дергать членов команды по каждому вопросу тоже не хочется.  
Пока что оставлю так. Мне кажется, что данных достаточно для проверки ответов. Если что потом подредактирую схемы.

Теперь моковые данные. Сгенерирую их в ChatGPT и проверю на правильность.
Данные сгенерировал. Дальше нужно накидать макет квиза.

Ментор сказала для хранения состояния использовать Store (Zustand), а я пока этот вопрос не изучал. Уже накидал примерный вид квиза с использованием useContext(). Эх, пойду изучать сторы.

Ознакомился с Redux. Завтра надо будет изучить Zustand.

## Итоги дня

### Выполненные задачи

- [x] Разработать JSON-схемы для отправки ответов и получения результатов теста;
- [x] Создать моковые данные для квиза;
- [ ] (new) Изучить библиотеки для хранения глобальных состояний (Zustand, Redux);
- [ ] Разработать начальный вид и логику компонента теста.

### Проблемы

- Отсутствие понимания логики обработки запросов на бекэнде;
- Отсутствие опыта использования менеджеров глобальных состояний в React;
- Отсутствие понимания, где и как хранить различные состояния приложения.

# 2026-02-18 (Zustand)

## Список задач на сегодня

- [ ] Изучить Zustand.

## Ход работы

Сегодня не так много свободного времени, да и пока не поставим Zustand, работу по Code Completion Widget не продолжить, поэтому ограничусь изучением Zustand.

Прошерстил документацию по Zustand, более-менее разобрался со Slices pattern. Теперь нужно решить, используем ли какое middleware, и дождаться, когда тимлид подключит нужные библиотеки в ветку develop.

Также не получается спокойно начать разработку и коммитить изменения из-за отсутствия структуры папок проекта, так как создание папок где попало вызовет лишь путаницу и конфликты при слиянии. Тимлид пока что был занят изучением роутинга и не мог все это настроить. Поэтому завтра, надеюсь, можно будет уже создавать хотя бы общие компоненты.

## Итоги дня

### Выполненные задачи

- [x] Изучить Zustand.

### Проблемы

- Отсутствие заранее составленного стека технологий, ввиду отсутствия опыта работы с React у всей команды;
- Отсутствие заранее продуманной структуры папок проекта (по той же причине).

# 2026-02-19

## Список задач на сегодня

--

## Ход работы

Сегодня наконец был поставлен Zustand и настроена маршрутизация со структурой проекта. Надеюсь, теперь работа пойдет полным ходом 😊.

К сожалению произошло это в середине дня, когда я уже ехал на учебу, поэтому не было времени ничего накодить. Вот, пишу этот дневник в 23:09, только вернувшись домой.

Завтра много дел, надо будет создать макет страницы для квизов, чтобы можно было уже в реальных условиях их разрабатывать.

## Итоги дня

### Выполненные задачи

--

### Проблемы

--

# 2026-02-20 (страница Practice)

## Список задач на сегодня

- [ ] создать макет страницы Practice;
- [ ] добавить скелетон при загрузке квиза;
- [ ] добавить сообщения с ошибками;
- [ ] получить одобрение Pull Request.

## Ход работы

Наконец появилась возможность что-то создать.

Накидал макет страницы, опираясь на дизайн, предложенный Кристиной (mightyprinces). Добавил лоадер в виде скелетона из компонентов Material UI, обернул сообщения ошибок в компонент Alert из Material UI. Надеюсь все правильно писал с точки зрения React (буду ждать ревью от ментора) и что дизайн вышел нормальным.

В целом, сделано не так много, но это позволит уже спокойно размещать компоненты на странице и видеть, как они вписываются в дизайн приложения.

Теперь жду результаты ревью. А пока нужно собираться и ехать на учебу :(

<hr>
upd от 20 числа:
забыл вчера закоммитить дневник.

В общем, вчера еще подняли вопрос, продолжать ли использовать Material UI, так как ментор сказала, что он довольно сложен в кастомизации. Однако, в результате голосования было решено продолжать им пользоваться. В следствие этого я задумался над переключением темы.

Уже сегодня (20 числа) изучил этот момент и как оказалось, чтобы переключение темы в MUI хорошо работало, нужно использовать только цвета, заданные через тему и избегать жестких констант. Также получил пару замечаний по коду, поэтому буду исправлять их и использование цветов.

## Итоги дня

### Выполненные задачи

- [x] создать макет страницы Practice;
- [x] добавить скелетон при загрузке квиза;
- [x] добавить сообщения с ошибками;
- [ ] получить одобрение Pull Request.

### Проблемы

- неуверенность в выбранном стеке технологий в виду отсутствия опыта.

# 2026-02-21 (страница Practice, Code Completion Widget)

## Список задач на сегодня

- [ ] исправить ошибки в работе над страницей /practice;
- [ ] заменить жесткие цвета через константы на гибкие через тему MUI;
- [ ] протестировать переключение темы и вид интерфейса в обоих режимах;
- [ ] поработать над своим компонентом.

## Ход работы

Ошибки исправил, цвета заменил, где это было возможно. Переключение темы тестировать не стал, так как нужно было как можно быстрее смерджить пулл реквест, да и особого смысла в этом не было.

Теперь надо начать работу над своим компонентом.

Создал прогрессбар и все таки решил попробовать переключить тему и... ничего не работает))) Переключению темы подвержены только цвета по типу primary, secondary, error и т.д., а оттенки серого задаются как есть. Нужно делать свою палитру, а иначе не продвинуться, ну или делать без стилизации, чисто функционал.

На сегодня, думаю, все, завтра уже буду делать функционал без стилей для своего компонента.

Решил еще поработать над темой, поигрался с конструкторм тем colorffy.com. Темная прям хорошая получилась после переноса на наш макет. Со светлой пока не особо. Лучше изначальный вариант оставить, как на макете.

## Итоги дня

### Выполненные задачи

- [x] исправить ошибки в работе над страницей /practice;
- [ ] заменить жесткие цвета через константы на гибкие через тему MUI;
- [x] протестировать переключение темы и вид интерфейса в обоих режимах;
- [ ] поработать над своим компонентом.

### Проблемы

- отсутствие гибкой и обширной палитры в Material UI.

# 2026-02-22 (темная тема)

## Список задач на сегодня

- [ ] доработать палитры для темной и светлой тем;
- [ ] перенести палитры в тему Material UI;
- [ ] добавить типографику в тему Material UI;
- [ ] создать кнопку переключения темы;
- [ ] пройти pull request.

## Ход работы

Начал с переноса палитры, которую сделал вчера на макет сайта в Figma. Смотрелось не плохо, только изменил пару цветов.  
Теперь нужно перенести все в тему в Material UI. Поискал, как это лучше сделать и получилось все без особых проблем. Единственный момент был с тем, как повесить стиль с градиентным фоном на body. Для этого нужно было добавить переписанное правило для body из CssBaseline, в которое вставить стиль фона из палитры:

```typescript
return createTheme({
  palette,
  typography,
  components: {
    MuiCssBaseline: {
      styleOverrides: (theme) => ({
        body: {
          backgroundImage: theme.palette.backgroundGradient,
          backgroundAttachment: "fixed",
          backgroundRepeat: "no-repeat",
          backgroundSize: "cover",
        },
      }),
    },
  },
});
```

Закинул пул реквест, жду комментариев команды.

На моё удивление, ушло довольно много времени, чтобы все это сделать.

Пул реквест одобрили. Объяснил в чате, как работать с темой и как добавлять новые цвета, надеюсь всем будет понятно и просто.

На следующей неделе уже надо плотно засесть за написание своего компонента Code Completion Widget, так как, по идее, все для этого уже готово (по крайней мере для начальной стадии, так как как завершать квиз и обрабатывать результаты пока не особо понятно. Думаю договоримся об этом как раз).

## Итоги дня

### Выполненные задачи

- [x] доработать палитры для темной и светлой тем;
- [x] перенести палитры в тему Material UI;
- [x] добавить типографику в тему Material UI;
- [x] создать кнопку переключения темы;
- [x] пройти pull request.

### Проблемы

- незнание того, как работать с темой в Material UI.

# 2026-02-23 (Code Completion Widget)

## Список задач на сегодня

- [ ] создать общий компонент навигации по квизу;
- [ ] настроить управление состоянием квиза.

## Ход работы

Создал общий компонент навигации с 2 кнопками "Назад" и "Следующий вопрос". Сначала хотел хранить управление в квизе, прокидывая лишь колбеки в навигацию, но потом понял, что это не удобно. В итоге перенес все в сам компонент навигации, а через props прокинул номер текущего вопроса, его сеттер и общее число вопросов. Позже, как разберемся с тем, как завершать квиз надо будет добавить и колбек для этого.

К квизу привязалось все нормально, но вот с самим квизом не все так просто. Мне нужен инпут для ввода ответа, который будет сохранять ответ, чтобы при навигации данные пользователем ответы появлялись в инпуте. Также надо где-то хранить все ответы за данный квиз.
Тут возникла проблема:

```
CodeCompletionWidget contains an input of type text with both value and defaultValue props. Input elements must be either controlled or uncontrolled (specify either the value prop, or the defaultValue prop, but not both). Decide between using a controlled or uncontrolled input element and remove one of these props.
```

Нельзя и использовать инпут для ввода значения и заполнять его значением по умолчанию.  
Убрал state для отдельного ответа и в value инпута начал ссылаться на значение из массива всех ответов. По началу была проблема с тем, что все ответы изначально undefined и при смене на пустую строку input переставал был uncontrolled, на что ругался React. Решил это, написав (вроде как, нормальная практика делать именно так):

```typescript
<input
  type="text"
  value={answers[currentQuestionNumber] ?? ''}
  onChange={handleAnswerInput}
/>
```

Все заработало как надо. Создал глобальный state для своего квиза и разделил все части виджета по своим файлам, чтобы оптимизировать перерисовку.

Столкнулся с проблемой переноса текста, посреди которого есть input. Оказалось, что дело не в наличии input, а просто в том, что я поместил все в тег `<pre>`, поэтому исправилось все стандартными строками css:

```css
white-space: pre-wrap;
word-break: break-word;
```

Всё, закончил! Я доволен результатом. Осталось настроить отправку и проверку ответов с отображением результатов, но это уже будет позже, когда обо всем договоримся и будет готов бекэнд.

## Итоги дня

### Выполненные задачи

- [x] создать общий компонент навигации по квизу;
- [x] настроить управление состоянием квиза.

### Проблемы

- управление состоянием в квизе, где нужна история ответов;

# 2026-02-24 (Code Completion Widget)

## Список задач на сегодня

- [ ] рефакторинг виджета;
- [ ] пройти PR.

## Ход работы

Просматривая Pull Request Алексея увидел у него формат ответов на вопросы квиза и понял, что когда делал свой виджет, забыл про то, что ответ должен иметь вид:

```json
{
  questionId: '1';
  payload: 'string';
}
```

А я сделал ответы просто массивом строк, что в целом нормально, но на случай перемешивания вопросов или просто для однозначности это не подходит. Поэтому пришлось немало переписать.

Запушил изменения. Осталось дождаться комментариев по PR.

Одобрили, все норм.

## Итоги дня

### Выполненные задачи

- [x] рефакторинг виджета;
- [x] пройти PR.

### Проблемы

- забыл про формат ответов пользователя 🤦;

# 2026-02-26 (Async Sorter Widget)

## Список задач на сегодня

- [ ] сделать ревью пулл реквеста Алексея;
- [ ] начать работать над Async Sorter Widget.

## Ход работы

Провел [ревью](https://github.com/Artem-WebDeveloper/tandem/pull/24#pullrequestreview-3858759548) пулл реквеста.

upd от 22:42:
К сожалению у Алексея не было возможности внести правки в пулл реквест, и не будет до завтрашнего дня. В связи с этим работа над новым компонентом откладывается. Надо будет на выходных плотно за него засесть.

## Итоги дня

### Выполненные задачи

- [x] сделать ревью пулл реквеста Алексея;
- [ ] начать работать над Async Sorter Widget.

### Проблемы

- задержка важного пулл реквеста.

# 2026-02-27 (Async Sorter Widget)

## Список задач на сегодня

- [ ] начать работать над Async Sorter Widget.

## Ход работы

Наконец был смерджен пулл реквест с типизацией квизов! Зато немного отдохнул от проекта и занялся, наконец, учебой :)

В целом Async Sorter по интерфейсу схож с Code Completion, поэтому "скопипастил" большую часть кода оттуда. Но вот теперь предстоит разобраться с самым сложным - Drag&Drop. Я его недолюбливаю еще с RSS Puzzle 😆, поэтому решил перебороть эту ненависть (ну, или усилить ее) через данный проект. В отличие от паззла, где все надо было делать на ванилле, в React есть библиотеки для работы с D&D.  
Изучил [статью](https://geekflare.com/dev/best-drag-and-drop-react-libraries/). По описанию подходит больше всего React DnD, также, возможно, может подойти Draggable (@shopify/draggable). Буду разбираться дальше.  
Спросил ChatGPT, какое решение подойдет под мой кейс, он также посоветовал React DnD, но еще и dnd-kit с react-beautiful-dnd. Судя по описанию самый простой вариант - react-beautiful-dnd, но я посмотрел [документацию](https://dndkit.com/react/quickstart) по dnd-kit и мне понравилась понятность данного пакета, к тому же он активно поддерживается в отличие от react-beautiful-dnd и не такой сложный, как React DnD. Буду использовать dnd-kit.

Чтобы вынести обработчик события D&D из Provider нужно типизировать событие, принимаемое функцией-обработчиком, а в dnd-kit, похоже не предусмотрен конкретный тип для такого события (есть что-то с таким именем, но ts ругается). пришлось выкручиваться через `type OnDragEnd = React.ComponentProps<typeof DragDropProvider>['onDragEnd'];`

Возникла проблема: не понятно, как синхронизировать состояние внутри зоны с блоками ответов и сами ответы в Zustand.  
Заменил useState на обычную константу, которая формируется из данных в состоянии Zustand и стало работать как надо.

Попробовал добавить перемещение между дропзонами по клику. К сожалению, это вызывает неприятный баг: блоки в начальной зоне перестают двигаться. Спасает взятие блока из целевой зоны и перетаскивание его в начальную зону, но это не то, что нужно)). Решил отказаться от перемещения по клику.

Еще немного пободался с обработкой отмены перетаскивания через escape, но, вроде бы как, все работает как часы.  
Также есть дурацкий баг с перемещением элементов на первое место в дропзоне. Решение этому я не нашел, в примерах из документации баг так же присутствует. В целом, он не особо критичный, так что можно смириться.

Завтра надо будет заняться стилизацией всего этого дела.

Вау, решил еще раз уточнить про кривые перестановки у ChatGPT и он посоветовал использовать метод разрешения коллизий `closestCorners`, в документации же были 3 других варианта и ни один не подошел, а этот с горизонтальным списком работает как надо

## Итоги дня

### Выполненные задачи

- [x] начать работать над Async Sorter Widget.

### Проблемы

- отсутствие встроенной типизации события в dnd-kit;
- сложности в синхронизации состояния в Zustand и состояния перетаскиваемых элементов;
- проблемы совместимости dnd-kit и кастомной обработки кликов по перетаскиваемым элементам для их перемещения.

# 2026-03-01 (Async Sorter Widget)

## Список задач на сегодня

- [ ] настроить стили для Drag&Drop компонента;
- [ ] добавить подсветку синтаксиса для кода в задании.

## Ход работы

Решил, что не очень удобно, когда код идет сплошным цветом. В Code Completion кода было не много и там это смотрелось нормально, но вот в Async Sorter много строчек, поэтому сложно воспринимать код без подсветки синтаксиса.  
Недолго погуглив решил попробовать react-syntax-highlighter. В codesandbox нашел проект для его тестирования и в результате перебора всех стилей подсветки пришел к таким вариантам: docco для светлой темы и railscasts для темной.

В Code Completion, к сожалению, подсветку синтаксиса не удалось добавить, так как SyntaxHighlighter не принимает как детей ничего, кроме строк, а мне нужно воткнуть input между двух кусков кода, да еще и так, чтобы это выглядело, как единое целое.

Все доделал и открыл pull request. Пришли замечания. Из наиболее важного - неправильная запись в state в Zustand. Мы не используем Immer, поэтому нужно всегда всё заменять на копии при перезаписи state, а у меня было просто записывание в текущий. К том же пришедший массив с ответом я не копировал, а вписывал ссылкой. Исправил это, а также переписал такую же функцию в store для CodeCompletion.

## Итоги дня

### Выполненные задачи

- [x] настроить стили для Drag&Drop компонента;
- [x] добавить подсветку синтаксиса для кода в задании.

### Проблемы

- неправильная работа с состоянием в Zustand.

# 2026-03-02 (Async Sorter Widget)

## Список задач на сегодня

- [ ] получить апрув пулл реквеста.

## Ход работы

Возникла проблема, связанная с использованием старой версии GitHub actions и Node.js в workflow. Я выкатил pr с фиксом и после его мерджа в develop решил подтянуть изменения и сделать rebase ветки с Async Sorter. По итогу после ребейза и синхронизации с удаленной веткой получил дубликат коммитов, так как их хэши поменялись после ребейза. Нужно было делать git push --force-with-lease, а я доверился VS Code и просто нажал Sync Changes. В следующий раз буду делать по-нормальному ))

PR просмотрен ментором и одобрен без нареканий.

## Итоги дня

### Выполненные задачи

- [x] получить апрув пулл реквеста.

### Проблемы

- неправильная работа с git при командной разработке.

# 2026-03-05

## Список задач на сегодня

- [ ] записать видео-презентацию компонента;
- [ ] изучить, как можно внедрить LLM в наше приложение.

## Ход работы

Пока бекэнд находится в разработке и, следовательно, нет возможности продолжить работу над начатыми компонентами квизов я задал в чат вопрос: "Чем пока что можно заняться?". На что получил предложение разобраться с тем, как можно прикрутить LLM к нашему приложению. Целю LLM будет отвечать на вопросы пользователя касательно программирования, давая пояснения и объясняя какую-то тему.  
Пока что есть концептуальный вопрос: нужно ли хранить историю сообщений/чатов, учитывая что модель, по сути, просто отвечает на вопросы, возникающие в моменте прохождения тестов? Нужно будет обсудить это с командой. К тому же, Алексей уже начинал работу над LLM, только она была для тестирования знаний пользователя, так что не знаю, стоит ли заморачиваться с еще одной моделью.  
Изучил документацию по [ollama](https://github.com/ollama/ollama-js). Выглядит довольно удобной в использовании с точки зрения разработки: простой и компактный синтаксис, есть поддержка стриминга ответа модели. Нужно разобраться, как ее настроить на контекст дачи ответов на вопросы по программированию, а также как управлять ее памятью/контекстом.

С 3 попытки записал видео ([ссылка](https://youtu.be/2bJJ8zzFmKo)). Вроде, неплохо рассказал.

По поводу Ollama. Мучался пытаясь отправить пробный запрос через postman, но по итогу сдался и создал проект для теста. Но из-за CORS запросы к Api блокировались и как оказалось нельзя просто использовать ollama на фронте 🙃. А мне хотелось легковесное решение, без бека и локального запуска модели. Придется искать что-то другое.

## Итоги дня

### Выполненные задачи

- [x] записать видео-презентацию компонента;
- [x] изучить, как можно внедрить LLM в наше приложение.

### Проблемы

- неясность в вопросе внедрения LLM (какую цель мы преследуем => какие требования к модели);
- сложность поиска доступного api для чата с LLM.

# 2026-03-06

## Список задач на сегодня

- [ ] изучить различные варианты LLM для внедрения в наше приложение.

## Ход работы

Решил обратиться к ChatGPT за советом по подбору LLM. Он предложил 2 типа решения:

- LLM реально работает в браузере (WASM/WebGPU);
- LLM работает через бесплатный API, но ты вызываешь его напрямую из фронта.

Первый вариант требует загрузки модели в память (100-500 МБ), а также скорость работы зависит от производительности устройства пользователя, что не вариант для нас, так как приложение ориентировано в том числе и на мобильные девайсы.  
Из второго варианта были предложены Together AI и Groq API. Together имеет лишь 1 бесплатную модель, которую даже нельзя бесплатно протестировать.  
С Groq опять та же проблема: нельзя обращаться к Api через фронт. Нужен прокси-бекэнд 🙄  
Ладно, пока что оставлю эту идею. Может, позже вернусь к ней.

## Итоги дня

### Выполненные задачи

- [x] изучить различные варианты LLM для внедрения в наше приложение.

### Проблемы

- невозможность внедрения LLM через Api без бекэнда.

# 2026-03-10

## Список задач на сегодня

- [ ] провести ревью пулл реквеста с компонентом Single Choice Widget.

## Ход работы

## Code Review

Провел ревью [PR #37: Feature/single choice](https://github.com/Artem-WebDeveloper/tandem/pull/37).

Указал на пару моментов, в частности, на необходимость использования глобального состояния и разделение компонента на части для избежания лишних перерисовок и удобства сопровождения. Также заметил неприятный для пользовательского опыта момент: при переключении между вопросами назад-вперед данные ответы переставали отображаться на интерфейсе.

Буду ждать ответов и обсуждать, если понадобится.

## Итоги дня

### Выполненные задачи

- [x] провести ревью пулл реквеста с компонентом Single Choice Widget.

### Проблемы

--

# 2026-03-12

## Список задач на сегодня

- [ ] изучить vitest;
- [ ] написать 5 тестов для моих компонентов.

## Ход работы

Начал с изучения документации по Vitest. Не особо в ней много информации о том, какие есть методы для тестирования и как ими пользоваться. Придется разбираться по-другому. В документации testing-library побольше полезной информации. Буду изучать ее.

Думаю, для начала написать пару тестов для общих компонентов QuizProgressBar и QuizNavigation, чтобы разобраться, так как пока не очень понятно, как и какие тесты писать для более сложных компонентов.

Для QuizProgressBar добавил проверку рендера без ошибок и проверку рендера с правильными значениями:

```typescript
describe('QuizProgressBar', () => {
  it('renders without errors', () => {
    render(<QuizProgressBar currentQuestionNumber={1} questionsCount={1} />);
  });

  it('renders with correct values', () => {
    render(<QuizProgressBar currentQuestionNumber={2} questionsCount={5} />);

    const currentQuestion = screen.getByText('Вопрос 2 из 5');
    const progress = screen.getByText('40%');

    expect(currentQuestion).toBeInTheDocument();
    expect(progress).toBeInTheDocument();
  });
});
```

Написал тесты для QuizNavigation. Получилось аж 10. Решил узнать, как можно сделать их компактнее, так как рендер компонента в каждом из них занимает по 8 строчек, при этом не сильно отличаясь от теста к тесту. Изучив вопрос, узнал про практику использования функции setup, которая подготавливает начальные данные для тестов. В нее как раз можно вынести рендер компонента. Также обновил тесты для QuizProgressBar, вынеся общее в setup.

## Итоги дня

### Выполненные задачи

- [x] изучить vitest;
- [x] написать 5 (13) тестов для моих компонентов.

### Проблемы

- скудность документации vitest для новичка;
- неопытность в написании тестов (в том числе для компонентов React).

# 2026-03-15

## Список задач на сегодня

- [ ] перевести виджеты в режим работы через api;
- [ ] обновить квизы под новую json-схему с переводами;
- [ ] обновить страницу Practice под новую json-схему с переводами;
- [ ] заменить кастомные Chip'ы на дефолтные из MUI для единообразия со страницей Library.

## Ход работы

Вчера согласовали данные квизов и загрузили в базу несколько примеров для перехода на api. Сегодня как начал писать код, заметил, что в данных "type": "code completion", а в коде ожидается "type": "code_completion". Ошибка будет исправлена в скором времени.

Когда настраивал .env файл столкнулся с тем, что в Vite его можно использовать без пакета dotenv, который я сначала скачал. Потом оказалось, что имя переменной всегда должно начинаться с VITE, чтобы можно было использовать ее.

Пока локализация не настроена решил в папку `configs` добавить файл `locale.config.ts` с текущей локалью, чтобы можно было извлекать по ней данные из JSON-схемы.  
Обновил все места, где используется локаль на странице Practice и в моих квизах. Для других квизов пока не настроен API и моковые данные не поддерживают локализацию, так что не вижу смысла с ними что-то делать.

Заменил свои кастомные цветные Chip'ы на стандартные из Material UI, чтобы сочеталось со станицей Library. В целом они стали больше вписываться в стиль приложения, так что не страшно.

## Итоги дня

### Выполненные задачи

- [x] перевести виджеты в режим работы через api;
- [x] обновить квизы под новую json-схему с переводами;
- [x] обновить страницу Practice под новую json-схему с переводами;
- [x] заменить кастомные Chip'ы на дефолтные из MUI для единообразия со страницей Library.

### Проблемы

- ошибка в согласовании данных для квизов;
- неумение работать с .env файлами.

# 2026-03-19

## Список задач на сегодня

- [ ] провести ревью компонента TrueFalseWidget;
- [ ] поработать над дашбордом.

## Ход работы

Сегодня лидер команды должен залить пр со своим компонентом для проверки. Надо будет глянуть.

Провел ревью. Компонент сделан очень круто. Не думал, что такой простой квиз можно сделать настолько визуально привлекательным и интересным. Надо будет и в своих компонентах добавить компонент Fade из Material UI.

До конца недели надо будет поработать с дашбордом (стили, скелетоны и т.д.). А то уже давненько ничего не делал по проекту, так как с бэкендом накладки.

Решил начать сегодня) Пока настроил стили как на макете и для графика приметил библиотеку ant-design-charts, которую используют в RS App. Документация на китайском, но ничего - с переводчиком в браузере разобрался как настроить стили у графика. Завтра надо будет внедрить такие графики для прогресса по сложностям. А также сделать скелетоны для всех карточек.

## Итоги дня

### Выполненные задачи

- [x] провести ревью компонента TrueFalseWidget;
- [x] поработать над дашбордом.

### Проблемы

- сложная документация на китайском у библиотеки.

# 2026-03-20 (Dashboard)

## Список задач на сегодня

- [ ] графики для прогресса по уровням сложности;
- [ ] миграция дашборда на моковое апи;
- [ ] скелетоны для всех карточек со статистикой;
- [ ] обработка ошибок загрузки статистики.

## Ход работы

Намучался с графиками. К сожалению настройка самой области диаграммы не очень гибкая: нельзя задать размеры графика и отступы от краев. Поэтому пришлось пробовать разное, чтобы нормально разместить 3 графика рядом и добавить заголовки к ним. По итогу ушел от прямоугольной формы и поставил стандартную круглую - так лучше смотрится, потому что у прямоугольной отступы по бокам большие остаются. Заголовок, встроенный в график имеет слишком большой отступ от него, что выглядит некрасиво, поэтому добавил свои заголовки в виде компонентов DifficultyChip.

Перенес получение данных в фиктивное апи. Также добавил с шансом 50 на 50 выброс ошибки, чтобы можно было тестировать работу и интерфейс при ошибках. Добавил скелетоны для состояний загрузки, а также окно с ошибкой и кнопкой "Попробовать еще раз", которая позволяет повторно запросить данные. У каждой карточки свой скелетон и окно ошибки, поэтому они могут работать независимо. (По началу вообще хотел, чтобы карточки из группы CommonStatistic были независимыми, но там из-за зависимого состояния одной карточки от других это было сложно реализовать с учетом окон с ошибками и повторными запросами данных, да и пришлось бы плодить разные функции с запросами на бек, что не особо имеет смысл)

Надо обсудить, какую еще информацию добавить можно. Графиков красивых в @ant-design/plots много. Вопрос только в бекэнде, так как у нас и так с ним накладки, потому что им занимается только одна Карина, у которой мало свободного времени. Так тчто не уверен, что стоит добавлять ей работы перед дедлайном.

Алексей выкатил пр с локализаций, в которой (очевидно) затронут и дашборд. Придется резолвить конфликты 😔. Ну, зато попрактикуюсь это делать, а то мало опыта в этом плане совсем. Из неприятного там еще и ошибки как-то унифицированы, придется и с ними что-то делать. В идеале такое с самого начала делать, чтобы потом все сразу с учетом локализации писали компоненты. Но ничего, учимся как раз на своих ошибках!

## Итоги дня

### Выполненные задачи

- [x] графики для прогресса по уровням сложности;
- [x] миграция дашборда на моковое апи;
- [x] скелетоны для всех карточек со статистикой;
- [x] обработка ошибок загрузки статистики.

### Проблемы

- проблема работы с зависимым состоянием одного из независимых элементов;
- низкая гибкость настройки области графика в @ant-design/plots;
- поздняя локализация.

# 2026-03-21 (Dashboard)

## Список задач на сегодня

- [ ] провести rebase ветки с дашбордом на обновленную ветку develop;
- [ ] переделать обработку ошибок под новый формат.

## Ход работы

Начал делать rebase. Я ожидал появление конфликтов в процессе, так как пр с локализацией затронул и файлы дашборда, но после нескольких удачно разрешенных конфликтов вылезла какая-то ошибка в VS Code: Could not apply [commit hash]...  
Вот содержимое лога гита:

```bash
2026-03-21 15:55:07.157 [info] Rebasing (3/6)
error: could not apply a20d30f... feat: add plots with progress by difficulty
hint: Resolve all conflicts manually, mark them as resolved with
hint: "git add/rm <conflicted_files>", then run "git rebase --continue".
hint: You can instead skip this commit: run "git rebase --skip".
hint: To abort and get back to the state before "git rebase", run "git rebase --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Could not apply a20d30f... # feat: add plots with progress by difficulty
...
2026-03-21 15:55:19.562 [warning] [GitFileSystemProvider][readFile] File not found - git:/c%3A/GitHub%20Projects/rs_school/tandem/src/pages/Dashboard/Dashboard.tsx.git?%7B%22path%22%3A%22c%3A%5C%5CGitHub%20Projects%5C%5Crs_school%5C%5Ctandem%5C%5Csrc%5C%5Cpages%5C%5CDashboard%5C%5CDashboard.tsx%22%2C%22ref%22%3A%22%22%7D
```

Пока что ничего не понятно. Почему file not found, если я его не удалял и не перемещал? Буду разбираться.

Если верить Chat GPT, то это просто такой же конфликт, как и те, что я уже решил, просто VS Code решил в этот раз попугать всплывающим окном с красным текстом O_O

Ага, теперь дошел до конфликта в package-lock.json. И какие изменения принимать? Chat GPT сказал, что после `npm i` файл все равно пересоберется, поэтому просто объединю изменения и понадеюсь на лучше ))

Ну, вроде все обошлось. Заменил ошибки на новый унифицированный вариант, поддерживающий локализацию и создал пулл реквест.

## Итоги дня

### Выполненные задачи

- [x] провести rebase ветки с дашбордом на обновленную ветку develop;
- [x] переделать обработку ошибок под новый формат.

### Проблемы

- неопытность исправления конфликтов в git.

# 2026-03-28 (Dashboard)

## Список задач на сегодня

- [ ] провести ревью 3 пулл реквестов;
- [ ] подготовиться к переходу дашборда на апи.

## Ход работы

Потихонечку подходим к завершению проекта, поэтому активно появляются пулл реквесты и надо их хорошенько просматривать.

В целом все хорошо, уже все разобрались, что да как. Не то, что в начале. Куда меньше замечаний пишется.

Единственное, что не особо получается писать лаконичный код. Компоненты некоторые довольно большими выходят и их сложно ревьюить. Надо бы над этим работать.

Также подготовился к переходу дашбора на апи. Было решено сделать 1 эндпоинт, вместо 3, поэтому переписывал компоненты. Пришлось отказаться от независимой обработки ошибок и отрисовки, но, возможно так даже лучше для пользователя. Завтра, надеюсь, апи будет готово.

## Итоги дня

### Выполненные задачи

- [x] провести ревью 3 пулл реквестов;
- [x] подготовиться к переходу дашборда на апи.

### Проблемы

- большие и непонятные компоненты на ревью;
- несогласованность изначальной идеи дашборда с беком.

# 2026-03-29 (Dashboard)

## Список задач на сегодня

- [ ] перевести дашборд на апи;
- [ ] добавить сброс состояния квизов, использующих Zustand при выходе из них.

## Ход работы

Уже вставал вопрос по поводу сохранения состояния квизов, которые используют global state менеджер, но в тот раз обсуждался контекст сохранения состояния после отправки, поэтому мы добавили сброс состояния при отправке ответа, но я тут случайно понял, что можно же выйти с квиза во время его прохождения, при этом состояние также сохраняется. Поэтому надо это исправить, добавив сброс состояния при размонтировании компонента.  
Создал issue, сделал фикс и залил PR.

Апи для дашборда готово. Изменения внес быстро, так как вчера подготовился. Нужно было лишь заменить имена пары полей и удалить старые файлы с функциями для запросов к апи. Создал пулл реквест.

## Итоги дня

### Выполненные задачи

- [x] перевести дашборд на апи;
- [x] добавить сброс состояния квизов, использующих Zustand при выходе из них.

### Проблемы

--
